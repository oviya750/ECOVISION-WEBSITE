const powerBiPages = ['dashboard.png', 'dashboard2.png', 'dash 3.png', 'dash 4.png', 'dash 5.png'];
let powerBiPage = 0;
function showPowerBiPage(index) { powerBiPage = (index + powerBiPages.length) % powerBiPages.length; const image = document.getElementById('powerbiImage'); image.src = `/static/images/${encodeURIComponent(powerBiPages[powerBiPage])}`; image.alt = `Power BI report page ${powerBiPage + 1}`; document.getElementById('powerbiCounter').textContent = `${powerBiPage + 1} / ${powerBiPages.length}`; document.querySelectorAll('.powerbi-tab').forEach((tab, tabIndex) => tab.classList.toggle('active', tabIndex === powerBiPage)); }
document.addEventListener('DOMContentLoaded', () => { const tabs = document.getElementById('powerbiTabs'); powerBiPages.forEach((page, index) => { const button = document.createElement('button'); button.className = 'powerbi-tab'; button.textContent = `PAGE ${String(index + 1).padStart(2, '0')}`; button.setAttribute('role', 'tab'); button.addEventListener('click', () => showPowerBiPage(index)); tabs.appendChild(button); }); document.getElementById('powerbiPrev').addEventListener('click', () => showPowerBiPage(powerBiPage - 1)); document.getElementById('powerbiNext').addEventListener('click', () => showPowerBiPage(powerBiPage + 1)); document.getElementById('powerbiFullscreen').addEventListener('click', () => { const image = document.getElementById('powerbiImage'); if (image.requestFullscreen) image.requestFullscreen(); }); showPowerBiPage(0); });
